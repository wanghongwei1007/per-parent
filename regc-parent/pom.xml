<?xml version="1.0" encoding="UTF-8"?>
<!--suppress ALL -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <parent>
        <groupId>com.kalix.per</groupId>
        <artifactId>per-parent</artifactId>
        <version>1.0.1-SNAPSHOT</version>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <groupId>com.kalix.per.regc</groupId>
    <artifactId>regc-parent</artifactId>

    <name>Kalix Per Regc</name>

    <packaging>pom</packaging>

    <properties>

    </properties>
    <modules>
        <module>per-regc-entities</module>
        <module>per-regc-api</module>
        <module>per-regc-dao</module>
        <module>per-regc-biz</module>
        <module>per-regc-rest</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.kalix.per.regc</groupId>
                <artifactId>per-regc-api</artifactId>
                <version>${project.per.version}</version>
            </dependency>
            <dependency>
                <groupId>com.kalix.per.regc</groupId>
                <artifactId>per-regc-entities</artifactId>
                <version>${project.per.version}</version>
            </dependency>
            <dependency>
                <groupId>com.kalix.admin.core</groupId>
                <artifactId>admin-core-api</artifactId>
                <version>${project.admin.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>